import * as React from 'react';

import { ErrorProps } from 'container/error/error.container';

const ErrorComponent: React.FC<ErrorProps> = ({ code, name, message }) => {
  return <></>;
};

export default ErrorComponent;
